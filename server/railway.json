{
  "$schema": "https://railway.app/v1.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["server/**"]
  },
  "deploy": {
    "startCommand": "node app.js",
    "rootDirectory": "server",
    "healthcheckPath": "/",
    "numReplicas": 1
  }
}
